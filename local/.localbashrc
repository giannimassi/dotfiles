#
# ~/.localbashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return


# Source files in localbashrc.d dir (included in gitignore because it
# should not be committed, since it can contain sensitive info)
for file in ~/.localbashrc.d/*.bashrc;
do
 source "$file"
done
